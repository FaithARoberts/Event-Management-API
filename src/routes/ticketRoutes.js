import express from 'express';
import * as venueController from '../controllers/ticketController.js';
import {} from '../middleware/validateTicket.js';

const router = express.Router();

router.get('/', );
router.get('/:id',);
router.post('/', );
router.put('/:id',);
router.delete('/:id',);

export default router;