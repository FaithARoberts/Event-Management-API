import express from 'express';
import * as eventController from '../controllers/userController.js';
import {} from '../middleware/validateEvent.js';

const router = express.Router();

router.get('/', );
router.get('/:id',);
router.post('/', );
router.put('/:id',);
router.delete('/:id',);

export default router;