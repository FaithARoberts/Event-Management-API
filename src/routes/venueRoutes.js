import express from 'express';
import * as venueController from '../controllers/userController.js';
import {} from '../middleware/validateVenue.js';

const router = express.Router();

router.get('/', );
router.get('/:id',);
router.post('/', );
router.put('/:id',);
router.delete('/:id',);

export default router;